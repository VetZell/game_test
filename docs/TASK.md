# Current Task

## Task ID
TASK-004

## Status
READY

## Priority
High

## Title
Проверить архитектуру репозитория и привести документацию к фактическому состоянию

## Goal
Провести проверяемый аудит текущей структуры проекта `VetZell/game_test`, зафиксировать реальную архитектуру frontend, backend, базы данных, Telegram-интеграции и deployment-конфигурации, а затем устранить расхождения между кодом и проектной документацией без изменения поведения приложения.

## Instructions
1. Начать работу от актуального `main` после merge PR #2.
2. Прочитать файлы в порядке, указанном в `AGENTS.md` и `docs/CODEX_PROTOCOL.md`.
3. Создать отдельную ветку для TASK-004.
4. Проверить фактическую структуру репозитория, включая:
   - frontend entry points, компоненты, состояние, API-клиент и конфигурацию;
   - backend entry points, маршруты, модели, схемы, настройки, Telegram auth и idempotency;
   - Alembic и способ подключения PostgreSQL;
   - Railway/deployment-файлы, переменные окружения и команды запуска;
   - тесты и существующие проверки.
5. Сопоставить фактическое состояние с `README.md`, `docs/AI_CONTEXT.md`, `docs/PROJECT_STATE.md`, `docs/ROADMAP.md`, `docs/TECH_DEBT.md` и другими релевантными документами.
6. Исправить только подтверждённые неточности и устаревшие утверждения в документации.
7. Добавить `docs/ARCHITECTURE.md`, если такого файла нет, либо актуализировать его. Документ должен кратко описывать:
   - назначение проекта;
   - основные каталоги и ответственность модулей;
   - поток Telegram Mini App → frontend → backend → PostgreSQL;
   - аутентификацию Telegram;
   - изменение игрового состояния и idempotency;
   - миграции и deployment;
   - границы текущей архитектуры и известные ограничения.
8. Не выполнять архитектурный рефакторинг и не менять runtime-поведение в этой задаче.
9. Не изменять API-контракты, игровую экономику, UI и схему базы данных.
10. Все выявленные проблемы, требующие изменения кода, записать в `docs/TECH_DEBT.md` с приоритетом и кратким обоснованием.
11. Обновить `docs/PROJECT_STATE.md` и `docs/ROADMAP.md` по результатам подтверждённого аудита.
12. Полностью заменить `docs/REPORT.md` отчётом TASK-004 согласно протоколу.
13. Изменить статус задачи на `DONE`, сделать commit, push и открыть отдельный PR в `main`.
14. Merge не выполнять.

## Validation
- `git status --short --branch`
- `git diff --check`
- `cd backend && pytest -q`
- `cd backend && python -m compileall .`
- `cd backend && alembic heads`
- `cd frontend && npm run build`

Если какая-либо команда не может быть выполнена из-за отсутствия зависимостей или окружения, точно зафиксировать причину в `docs/REPORT.md` и не заявлять об успешной проверке.

## Acceptance Criteria
- Реальная архитектура репозитория проверена по исходному коду и конфигурации.
- `docs/ARCHITECTURE.md` существует и соответствует фактическому состоянию проекта.
- Устаревшие или неверные утверждения в проектной документации исправлены.
- Проблемы, требующие будущих изменений кода, записаны в `docs/TECH_DEBT.md`, но код в рамках этой задачи не рефакторился.
- Runtime-поведение, API, UI, игровая экономика и схема базы данных не изменены.
- Проверки выполнены и их реальные результаты указаны в `docs/REPORT.md`.
- Создан отдельный PR в `main`.
- После завершения статус задачи изменён на `DONE`.

## Restrictions
- Не добавлять `frontend/node_modules/` и другие generated-файлы.
- Не выполнять merge.
- Не вносить функциональные изменения.
- Не делать несвязанный рефакторинг.
- Не продолжать работу после установки статуса `DONE`.
