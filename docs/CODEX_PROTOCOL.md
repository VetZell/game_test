# Codex Workflow Protocol

## Purpose
Этот протокол связывает ChatGPT, пользователя и Codex через файлы GitHub.

## Roles
- **ChatGPT**: анализирует проект и записывает следующую задачу в `docs/TASK.md`.
- **Codex**: читает задачу, выполняет её, тестирует изменения и записывает результат в `docs/REPORT.md`.
- **Пользователь**: запускает Codex короткими командами и явно разрешает merge.

## Automatic startup synchronization

При точной команде `выполни` Codex обязан самостоятельно выполнить перед чтением задачи:

```bash
git fetch origin
git checkout main
git reset --hard origin/main
```

Пользователь не должен каждый раз вводить эти команды вручную.

После синхронизации Codex читает обязательные файлы в установленном порядке. Команда `cat docs/TASK.md` не обязательна: разрешён любой надёжный способ чтения файла в доступной среде.

Для команды `слей` этот hard-reset startup не используется, потому что завершённые `docs/TASK.md` и `docs/REPORT.md` могут находиться в task branch или Pull Request. При merge применяется отдельный порядок проверки ниже.

## Short commands

### `выполни`
Команда означает:
1. Автоматически синхронизировать локальный `main` с `origin/main` по обязательной startup-последовательности.
2. Прочитать все обязательные файлы по порядку.
3. Выполнить только активную задачу из `docs/TASK.md`, если её статус `READY`.
4. Выполнить проверки из `Validation`.
5. Обновить `docs/REPORT.md` и необходимую проектную документацию.
6. Сделать commit, push и создать или обновить Pull Request, когда это доступно.
7. После успешного завершения изменить статус задачи на `DONE`.
8. Merge не выполнять.

Если статус задачи уже `DONE`, при команде `выполни` Codex ничего не меняет и сообщает, что активной задачи нет.

### `слей`
Команда является явным разрешением пользователя на merge завершённого Pull Request.

Перед merge Codex обязан проверить:
1. В `docs/TASK.md` task branch или Pull Request указан статус `DONE`.
2. В `docs/REPORT.md` task branch или Pull Request указаны `Status: SUCCESS` и `Safe To Merge: YES`.
3. В отчёте указан реально существующий Pull Request.
4. Pull Request открыт, направлен в `main` и доступен для merge.
5. Все обязательные проверки задачи завершились успешно.
6. Нет незакоммиченных изменений, относящихся к задаче.

Если проверки пройдены, Codex:
1. Выполняет обычный merge или squash merge без force push и переписывания истории.
2. Переключается на `main`.
3. Выполняет `git fetch origin` и `git pull --ff-only origin main`.
4. Проверяет, что Pull Request имеет статус merged.
5. Сообщает merge commit SHA и текущий HEAD `main`.
6. Останавливается и ждёт следующую задачу.

Если хотя бы одна проверка не пройдена, Codex не выполняет merge, сообщает точную причину и останавливается.

## Required reading order
После автоматической синхронизации перед каждой задачей Codex обязан прочитать:
1. `AGENTS.md`
2. `docs/CODEX_PROTOCOL.md`
3. `docs/AI_CONTEXT.md`
4. `docs/PROJECT_STATE.md`
5. `docs/TECH_DEBT.md`
6. `docs/TASK.md`

## Task execution rules
1. Выполнять задачу только когда в `docs/TASK.md` указано `Status: READY`.
2. Использовать последнюю версию `docs/TASK.md` как единственный источник истины.
3. Не расширять объём задачи без необходимости.
4. Не удалять работающий функционал.
5. Не менять API, игровой баланс или архитектуру вне требований задачи.
6. Не выполнять merge без точной команды пользователя `слей`.
7. Работать в отдельной ветке, если интерфейс Codex позволяет создать PR.

## Required completion steps
После выполнения Codex обязан:
1. Запустить проверки из раздела `Validation` задачи.
2. Полностью заменить содержимое `docs/REPORT.md` актуальным отчётом.
3. Обновить `docs/PROJECT_STATE.md`, если состояние проекта изменилось.
4. Обновить `docs/TECH_DEBT.md`, если появился или устранён технический долг.
5. Обновить `docs/ROADMAP.md`, если завершён этап.
6. Добавить запись в `docs/CHANGELOG.md` для значимого изменения.
7. Сделать commit и, когда доступно, открыть Pull Request.
8. Указать реальный commit SHA и ссылку/номер PR в отчёте. Не писать «PR подготовлен», если он фактически не создан.

## Report requirements
`docs/REPORT.md` должен содержать:
- Task
- Status: SUCCESS, PARTIAL или FAILED
- Summary
- Files Changed
- Problems Found
- Problems Fixed
- Tests с точными командами и результатами
- Risks
- Technical Debt
- Safe To Merge: YES или NO
- Commit / PR

## Failure handling
Если операция невозможна из-за доступа, сети, зависимостей или неоднозначности:
- не имитировать успех;
- остановить опасные действия;
- записать точную ошибку и выполненные шаги в `docs/REPORT.md`;
- установить `Safe To Merge: NO`.

## End state
После успешного завершения Codex меняет статус в `docs/TASK.md` с `READY` на `DONE`, не стирая формулировку задачи.
